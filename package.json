{
  "name": "dashboard",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/dashboard.git" },
  "scripts": {
    "dev": "pnpm --filter @dashboard/web dev",
    "build": "pnpm --filter @dashboard/web build",
    "preview": "pnpm --filter @dashboard/web preview",
    "typecheck": "pnpm --filter @dashboard/web exec tsc -b",
    "test": "pnpm --filter @dashboard/web exec tsc -b"
  }
}
